package com.ceylonGo;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

@SpringBootTest
class CeylonGoApplicationTests {

    @Test
    void contextLoads() {
        // Verifies the Spring context wires up correctly (all beans, JPA
        // repositories, security config, etc.) - run with a MySQL instance
        // available and matching src/main/resources/application.properties.
    }
}
